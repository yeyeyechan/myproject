/*#include <iostream>
#include <algorithm>
#include<math.h>
nb thelist[100];
nb thelist2[900];
typedef struct numbers {
	int num;
	int strike;
	int ball;

} nb;

int firstcal(int hun, int ten, int one, int strike, int ball) {
	if (strike == 0) {
		if (ball == 0) {
			for
		}
	}
}

int main() {
	int count;
	
	cin >> count;

	for (int i = 0; i < count; i++) {
		cin >> thelist[i].num;
		cin >> thelist[i].strike;
		cin >> thelist[i].ball;

	}
	int maxindex = 0;
	int max = thelist[0].strike;
	for (int i = 0; i < count; i++) {
		if (max < thelist[i].strike) {
			maxindex = i;
			max = thelist[i].strike; }
	}
	
	int a;
	int b;
	int c;
	a = max / 100;
	b = (max%100)/10;
	c = (max%100)%10 ;


}*/